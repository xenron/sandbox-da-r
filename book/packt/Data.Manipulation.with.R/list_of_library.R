library(xlsx) # dependes xlsxjars and rJava with Java runtime installed in system
library(foreign)
library(lubridate)

library(plyr)
library(reshape)

library(RODBC)
library(filehash)
library(ff)
library(sqldf)

# all of the above library should be installed along with their dependent libraries
